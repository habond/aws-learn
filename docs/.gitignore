# Build system dependencies
build/node_modules/
build/package-lock.json

# Build cache
.cache/

# Optional: Uncomment if you want to only commit templates (not generated HTML)
# lessons/*.html
# index.html
